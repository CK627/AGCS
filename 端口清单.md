# AGCS 项目端口清单

> 按三类设备汇总：无人机、中枢地面站、地面机器人。
> 以各模块 `config.py`、`drone_config.py`、`task_server.py` 中的实际配置为准。

## 一、无人机（Dron）

| 端口 | 协议 | 用途 | 说明 |
|------|------|------|------|
| 20000 | HTTP | 无人机仪表盘（网页端） | 监听 `127.0.0.1`，见 `Dron/Dashboard/backend/config.py` |
| 14550 | UDP | pymavlink MAVLink 数传 | 监听 `udpin:0.0.0.0:14550`，见 `Dron/Code/drone_config.py` |
| 8080 | UDP | QGC 数传 | 与 pymavlink 的 14550 互斥，只能占用其一 |
| 554 | TCP | RTSP 图传 | 机载摄像头，`rtsp://192.168.1.10:554/...` |
| — | 串口 | USB 直连 | `COM3`（Windows）或 `/dev/ttyUSB0`（Linux），波特率 57600 |

SITL 仿真调试时用 `udpout:127.0.0.1:14550`（主动向地面站发送，而非监听）。

## 二、中枢地面站（GroundStation）

| 端口 | 协议 | 用途 | 说明 |
|------|------|------|------|
| 20001 | HTTP | 地面站统一仪表盘（网页端） | 监听 `0.0.0.0`，见 `GroundStation/Dashboard/backend/config.py` |
| 14550 | UDP | pymavlink 数传监听 | 接收无人机 MAVLink 数据（`DRONE_MAVLINK`） |
| 554 | TCP | RTSP 拉流 | 从无人机图传 `192.168.1.10` 拉取画面 |
| 5000 | TCP | 机器人任务/状态代理 | 指向机器人 `http://192.168.1.101:5000`（`ROBOT_URL`） |
| 8000 | TCP | YOLO 检测 API | 远程调用检测服务，接口 `/detect`（教学示例，独立服务未在仓库落地） |
| 8554 | TCP | MediaMTX 视频二次转发 | RTSP 流再发布，`rtsp://地面站IP:8554/drone` |
| 5000 | UDP | GStreamer 视频流转发 | 机载视频流 UDP 推流（与机器人 HTTP 5000 同号不同协议） |

## 三、地面机器人（GroundRobot）

| 端口 | 协议 | 用途 | 说明 |
|------|------|------|------|
| 20002 | HTTP | 地面机器人独立仪表盘（网页端） | 监听 `0.0.0.0`，见 `GroundRobot/Dashboard/backend/config.py` |
| 5000 | HTTP/TCP | 机器人对外服务 | `task_server.py` 的 `start_server(port=5000)`，跑在树莓派上 |

机器人 5000 端口对外提供三个接口：

| 接口 | 方法 | 说明 |
|------|------|------|
| `/task` | POST | 接收地面站下发的任务坐标 |
| `/status` | GET | 返回机器人状态（地面站仪表盘轮询） |
| `/video.mjpeg` | GET | 机器人摄像头 MJPEG 视频流 |

## 四、公共与跨设备端口

| 端口 | 协议 | 用途 | 说明 |
|------|------|------|------|
| 22 | TCP | SSH 远程调试 | 各设备通用，仅内网/VPN，不建议公网暴露 |

## 五、三个仪表盘端口对照

| 模块 | 端口 | 监听地址 |
|------|------|----------|
| 无人机 | 20000 | `127.0.0.1` |
| 中枢地面站 | 20001 | `0.0.0.0` |
| 地面机器人 | 20002 | `0.0.0.0` |

三个网页服务端口刻意错开，便于在同一台电脑上同时运行互不冲突。

## 六、注意事项

1. **端口 5000 复用**：机器人 HTTP 服务用 5000（TCP，跑在树莓派），GStreamer
   机载视频转发也用 5000（UDP，在地面站收发）。协议不同、主机不同，理论上不
   冲突，但联调时容易混淆，建议视频转发改用其他端口。

2. **8080 与 14550 互斥**：QGC 数传监听 8080，pymavlink 监听 14550，同一 UDP
   端口同一时间只能被一个程序占用，两者需在数传端分别加为目标。

3. **地面站 IP 需统一**：文档中地面站 IP 出现过 `192.168.1.100` 和
   `192.168.1.123` 两个值，实际以路由器 DHCP 按 MAC 绑定的地址为准。

4. **YOLO API 8000**：目前只存在于教学示例代码中，仓库内没有独立服务文件；
   若正式部署需单独实现并确认端口。

## 七、关键配置文件位置

| 模块 | 文件 | 主要端口配置 |
|------|------|--------------|
| 无人机仪表盘 | `Dron/Dashboard/backend/config.py` | `FLASK_PORT = 20000` |
| 无人机连接 | `Dron/Code/drone_config.py` | `CONNECTION = udpin:0.0.0.0:14550` |
| 中枢地面站仪表盘 | `GroundStation/Dashboard/backend/config.py` | `DASHBOARD_PORT = 20001` |
| 地面机器人仪表盘 | `GroundRobot/Dashboard/backend/config.py` | `DASHBOARD_PORT = 20002` |
| 机器人对外服务 | `GroundRobot/Code/communication/task_server.py` | `start_server(port=5000)` |
