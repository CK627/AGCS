# 地面站中枢仪表盘配置（GroundStation/Dashboard/data/config.yaml）
# 保存即热重载生效（端口除外：端口修改后需重启服务）

# ---------- 无人机电脑端 ----------
drone:
  # 无人机电脑端仪表盘地址（中枢从这里拉图传画面；
  # 不在同一台电脑时填那台电脑的局域网 IP；地面站也会通过 UDP 20003
  # 广播自动发现，这里的地址只是兜底）
  url: http://10.194.228.81:20000
  # 无人机画面开关
  video: true

# ---------- 地面机器人（可选） ----------
robot:
  url: http://10.194.228.89:5000
  video: true

# ---------- 仪表盘端口 ----------
dashboard:
  hub_port: 20001     # 地面站中枢端口
